# osintgpt corpus
#
# What this project has been told to index. Nothing is indexed because it
# happened to be on disk — a file or folder appears here or it is not corpus.
# Structured formats must name which of their fields carry content.

[[source]]
path = "D:/i/tools/osintgpt/examples/data/generated/case/material/prose"

[[source]]
path = "D:/i/tools/osintgpt/examples/data/generated/case/material/records/events.csv"

[source.fields]
content = [
    "summary",
]
metadata = [
    "record_id",
    "script",
]
timestamp = "reported_at"
identity = "record_id"
